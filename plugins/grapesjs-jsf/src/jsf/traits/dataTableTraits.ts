import all from "./allTraits"

export default [
    all.id,
    all.name,
    all.for,
    all.placeholder,
    all.textValue, /* our custom trait type in ExpressionTrait.ts */
    all.disabled,
    all.rendered,
    all.required,
    all.ajax
];